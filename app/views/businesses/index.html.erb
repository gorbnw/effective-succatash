<div class="search-area">

  <%= form_tag search_businesses_path, method: :get, class: "search-bar" do %>
    <%= text_field_tag 'search[term]' %>
    <%= text_field_tag 'search[location]' %>
    <%= submit_tag 'search' %>
  <% end %>
</div>

<% if @businesses %>
  <div class="business-list-div">
    <h2 class="business-header">Businesses</h2>
    <ul class="business-list">
      <% @businesses.each do |business| %>
        <li class="business">
<<<<<<< HEAD
          <div class="content">
            <div class="business-photo">
              <img src="<%= business['image_url'] %>" width="150">
            </div>
            <% categories = [] %>
            <% business['categories'].each do |category| %>
              <% categories << category["title"] %>
            <% end %>
            <div class="index-details">
              <p class="name list-item"><%= link_to business["name"], "/businesses/#{business['id']}" %></p>
              <p class="list-item"><%= categories.join(", ") %></p>
              <p class="phone list-item"><%= business["display_phone"] %></p>
              <p class="list-item"><%= business["location"]["display_address"].join(", ") %></p>
            </div>
          </div>
          <%= render partial: 'doodad', locals: {business: business} %>
=======
          <p><span class="name"><%= link_to business["name"], "/businesses/#{business['id']}" %></span><span class="phone"><%= business["display_phone"] %></span><%= business["location"]["display_address"].join(" ") %></p>
>>>>>>> e47f95d86d6f1b61cef7b3d975ec3017d20122a5
        </li>
      <% end %>
    </ul>
  </div>
<% end %>
